import { NgModule } from '@angular/core';
import { FirePipe } from './fire.pipe';
import { OrderDatePipe } from './orderDate.pipe';

@NgModule({
  declarations: [FirePipe, OrderDatePipe],
  providers: [FirePipe, OrderDatePipe],
  exports: [ FirePipe, OrderDatePipe
  ]
})
export class ServiceModule { }
